# Defer game-service restarts until a planned maintenance window.
# Security packages still update; needrestart continues reporting pending restarts.
$nrconf{override_rc}->{qr(^mc-luck-defense\.service$)} = 0;
$nrconf{override_rc}->{qr(^steve-td\.service$)} = 0;
$nrconf{override_rc}->{qr(^steve-td-proxy\.service$)} = 0;
